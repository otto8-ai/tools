Name: neonctl
Type: context
Share Tools: sys.exec
Share Context: github.com/gptscript-ai/context/os
Description: Help the user interact with neon.tech through the neonctl cli.

#!/bin/bash

if ! command -v neonctl; then
  echo "The user does not have neonctl installed or it is not available on the PATH."
  echo "Recommend the user install and provide instructions based on their OS."
else 
  neonctl --help
fi
